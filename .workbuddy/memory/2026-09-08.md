# 2026-09-08

## 拉取项目
- 从 https://github.com/liusu239113/chenguang-skylines 克隆到 D:\game\dushi（main 分支，HEAD 285ffed，51 个受版本控制文件）。
- 项目：都市天际线：晨光 —— UrhoX Lua 竖屏城市建造手游，入口 scripts/main.lua。

## 环境坑
- 全局 git 配置了 http.proxy/https.proxy = http://127.0.0.1:65532，但该端口无进程监听，导致 clone 报 "Failed to connect to github.com port 443 via 127.0.0.1"。
- 解决办法：直连可用，用 `GIT_CONFIG_COUNT=2 GIT_CONFIG_KEY_0=http.proxy GIT_CONFIG_VALUE_0= GIT_CONFIG_KEY_1=https.proxy GIT_CONFIG_VALUE_1= git <cmd>` 临时清空代理即可。
- 全局存在 url.insteadof 规则，把 https://github.com/ 重写为带 token 的 URL，因此 git 操作能免密；仓库 .git/config 里存的是干净 URL（无明文 token）。
- github.com 间歇性连不上（curl 返回 000/超时），但 api.github.com 一直通。push 失败时可用 Contents API（PUT /repos/{o}/{r}/contents/{path}）直接提交；下载 artifact zip 要用 curl -L（urllib 会把 Authorization 带到 Azure 重定向导致 401）。

## Android 原生移植（Kotlin）
- 新增 android/ 目录：Kotlin + Jetpack Compose（AGP 8.5.2 / Kotlin 1.9.25 / Compose Compiler 1.5.15 / BOM 2024.09.03 / Gradle 8.9 / compileSdk 35 / minSdk 26）。
- 结构：Config.kt、world/World.kt、world/Growth.kt、GameData.kt、world/MapRenderView.kt（Canvas 2D 渲染+相机+手势）、Sfx.kt（SoundPool）、ui/UIHelper.kt + ui/screens/{MainMenu,MapScreen,NewspaperScreen}.kt、MainActivity.kt。
- 移植原则：1:1 对齐 Lua 原版（数值、噪声 Long 溢出、坐标 1-based、渲染顺序、微立体、车辆、政策 daysLeft 取 cooldown 等）。
- 坐标系：onDraw 内 canvas.scale(density)，逻辑单位 = dp，与 Lua 逻辑像素对齐；触摸坐标除以 density。
- 资源：字体 ZCOOLKuaiLe 放 assets/fonts（Canvas 用）+ res/font（Compose 用）；5 个 ogg 放 res/raw。

## CI
- .github/workflows/android.yml：ubuntu-latest + JDK17 + 手动装 Gradle 8.9（不用 wrapper jar）+ assembleDebug + upload-artifact + softprops/action-gh-release。
- 踩坑记录：Compose Compiler 1.5.15 要求 Kotlin 1.9.25（1.9.24 会报错）；顶层扩展函数需显式 import；staticCompositionLocalOf 要写泛型；类内不能 const val；GITHUB_TOKEN 默认无 release 写权限 → release 步骤加 continue-on-error。
- 首次成功的 APK：run 34178652782（BUILD SUCCESSFUL），artifact 10038157760，已下载到 output/chenguang-skylines-debug.apk（12MB，7 个 dex）。

## 真机部署（小米无线调试）
- 小米手机通过无线调试连上：`adb mdns services` 发现 192.168.2.130:45335，`adb connect` 成功。
- 首次 install 报 INSTALL_FAILED_USER_RESTRICTED，用 `adb install -r -t` 后 Success；`monkey -p 包名 1` 启动。
- 包名 com.chenguang.skylines。小米需在开发者选项开启「USB 安装」才能装。

## 玩法扩充（第二批，用户要求"深度/几十小时可玩"）
- 基础设施：电力(风电/太阳能/燃煤电厂)、供水(水塔/水泵站)、垃圾(垃圾场)、医疗(医院)、教育(中学)、消防(消防站)，ServiceDef 加 category/unlockPop/pollution。
- 覆盖系统：World.coverage()/isCoveredBy()，缺电缺水垃圾堆积 → 满意度惩罚 + 缺电产业收入打折 + 缺水人口停滞。
- 覆盖热力图：MapRenderView.overlay（power/water/garbage/health/education/safety），绿=覆盖红=缺。
- 里程碑：CITY_LEVELS 加 reward，晋级给拨款；设施 unlockPop 锁定。
- RCI 三税率（5~15%），政策面板滑块；超基准扣满意度。
- 火灾：无消防覆盖建筑每日概率烧毁。
- 贷款：LOAN 800万借入/日还30/冷却90。
- 成就：10 个（人口/建筑/资金/满意度），达成发新闻+奖金。
- 微立体增强：BUILD sideShade 0.6/roofLight 1.15、GROWN_H 0.5/0.9/1.4、hpx 系数 0.55、窗户 cell>=14 更密。
- 改名：用户定名「都市建设模拟：山水之间」（Config.TITLE / app_name / MainMenu / 帮助文案已改，包名暂保留 com.chenguang.skylines）。
- 新图标：ImageGen 生成山水城市图标，Pillow 缩放到 mipmap 各密度 ic_launcher.png，删除了旧 adaptive icon。
- 贷款系统：LOAN 800万借入/日还30/冷却90，政策面板按钮 + 数据面板显示。
- 成就系统：10 项（人口/建筑/资金/满意度），每日检查达成发新闻+奖金。
- 用户明确要求先出策划案再动手：已写 docs/游戏策划案.md（对标端游，规划 30~60h 进度曲线 + 待实现批次：交通/产业链/教育/环境/市民/事件/任务/存档/难度/昼夜/地标）。后续按策划案批次推进，不再盲目推送。

## 策划案 v2 + 第3批（存档/命名/难度）
- 用户嫌第一版策划案"太浅"，重写为 GDD v2（21 章，几千字），覆盖存档/命名/交通/产业链/教育/市民/环境/政策/事件/任务/贸易/昼夜/报表/路线图。
- 游戏名最终定为「都市建设模拟：山水之间」（用户输入）。
- 第3批已实现并部署：
  - 主菜单重构：继续游戏/新游戏/存档管理/沙盒/帮助。
  - 新游戏流程：城市命名(TextField) + 地图种子(随机/输入) + 难度三选(轻松/标准/困难，incomeMul/upkeepMul/eventMul)。
  - SaveManager：3 槽 JSON 存档（World grid 改动 + CityState + Growth.simTime + 种子/难度/沙盒），org.json 序列化到 filesDir。
  - 城市名贯穿：顶栏 Chip、简报标题、奠基新闻。
  - 暂停菜单（右上角 ≡）：继续/保存/保存并返回主菜单；每月自动存档。
  - 沙盒：无限资金 + 全解锁。
  - MainActivity 主循环 paused 时冻结模拟。
- 部署：CI run 34181444732 成功，APK 已下载并装到小米（卸载旧版重装，签名不匹配需 uninstall 后 install；小米弹窗确认后 Success）。
- 注意：CI 每次全新 ubuntu runner 会重新生成 debug keystore，导致不同 run 的 debug APK 签名不同，升级安装需先 uninstall。

## 用户 bug 反馈大改（第四批修复）
- 推平：bulldoze 增加清除分区(zone→none)，GameData 加 "zone" 分支（不退款）；底栏推平万能。
- 服务选择 bug：selectMode 重做，点「服务」总是重开抽屉（旧逻辑 selService!=null 时不再显示抽屉导致"只能选公园"）；点「道路」也开抽屉选单/双车道。
- 商业不长楼：Growth.computeDemand 商业需求 c = pop*0.55+25-comCap（原为 pop*0.55-comCap，开局=0）。
- 水域：颜色改蓝(112,168,196/100,156,186)，河流移到右边缘(riverX=cols-2=46)，湖泊移左上角(5,4)，colAve 第三条改 0.78（原 riverX+4 会越界）。
- 道路渲染：单车道灰中线+双车道黄中线+路口红绿灯(simTime 红绿闪烁)；roadLocal 改灰(214,210,198)；ROAD name 改「单车道/双车道」。
- 建筑名：成长建筑按坐标确定性取名(Config.NAMES 住宅/商铺/工厂前后缀池)，渲染在建筑框内缩放不超界。
- 覆盖热力图：重做为「设施覆盖范围圈(半透明绿圈) + 建筑绿/红 + 设施蓝」，可见性大幅提升。
- 水域波纹改两条白色短线。帮助文案更新推平/服务说明。
- 部署：run 34182567859 成功，卸载重装到小米，启动无崩溃。

## 用户强烈不满 → 停止写码，先出重构策划案
- 用户批评"摆设感"：人口恒0、资金无来源、车乱走无目的地、无车道/穿模、红绿灯摆设、无昼夜天空路灯、模型不立体、电水无来源、无对外连接、无机场火车站、事件无逻辑。
- 已写 docs/重构策划案.md（合理性专项），11 个问题逐条给因果链方案，分 A~G 阶段（人口经济→供电供水沿路网→交通重做→昼夜→立体→枢纽→事件）。
- 决策：用户要"先策划案再动手"，本轮不写代码不推送，等拍板。
